# DESIGN.md — Chloe Flora 花艺工作室管理后台

## 气质与意象

巴黎左岸高级定制花店的后场账房：手工装订的米色亚麻账本，花材卡片整齐排列在骨瓷托盘上，花剪与缎带各归其位。
画面里没有喧闹的促销色块——只有米白底色、发丝般的分隔线、以及爱马仕橙像花束中那枝点睛的火焰百合，只在关键动作处出现。
整体气质：安静、克制、有手作温度的奢华秩序感。品牌主张「Flowers, quietly luxurious.」

## 视觉策略

- 摄影/图像方向：自然光、低饱和、米色背景的花艺静物；后台不使用装饰插画与渐变。
- 图形语言：发丝线分隔（1px #EAE3D9）+ 大留白，卡片为骨瓷白，边框极浅，不用重投影。
- 橙色面积占比 ≤ 5%：只用于主按钮、选中态、品牌字标、关键数字。
- 后台框架（侧边栏/导航/表单）使用固定品牌 Token；「设计风格配置」页编辑的是小程序前端 Token，用手机样机实时预览，二者不混用。

## Design Tokens

### 色彩（后台固定品牌色）
- 背景米白 `--cf-bg: #F7F3ED`（意象：未漂白亚麻）
- 卡片骨瓷白 `--cf-card: #FFFFFF`
- 爱马仕橙（主色）`--cf-primary: #E8830C`，hover 加深 `#D4740A`
- 三级文字：标题 `--cf-text-1: #2B2622`（焙茶浓色）/ 正文 `--cf-text-2: #6B625A` / 辅助 `--cf-text-3: #A39A90`
- 发丝线 `--cf-line: #EAE3D9`；输入框底 `#FBF9F5`
- 成功（去饱和墨绿）`#3D7A5A`；危险（酒红）`#A8453B`；警告（赭石）`#B07D2B`

### 小程序可配主题色（site_settings.theme 默认值）
- `theme_color: #E8830C`（主题色/按钮）
- `bg_color: #F7F3ED`（页面底色）
- `text_primary: #2B2622` / `text_secondary: #6B625A` / `text_tertiary: #A39A90`

### 字体
- 展示标题（品牌名/页头/数字）：`Noto Serif SC` + `Cormorant Garamond`（英文），字重 500/600，大字距克制
- 正文/UI：`Noto Sans SC`、PingFang SC 系统无衬线回退
- 价格数字：`Cormorant Garamond` tabular，保留两位小数
- 字号层级：页面标题 24px serif / 区块标题 16px / 正文 14px / 辅助 12px；行高 1.6~1.7

### 间距与圆角
- 4px 栅格基准；页面外边距 32px；卡片内边距 24px；表单项纵向间距 20px
- 圆角：卡片 12px，按钮/输入 8px，标签 6px；禁止超过 12px

### 阴影
仅一层极浅暖阴影：`0 1px 2px rgba(43,38,34,0.04), 0 8px 24px rgba(43,38,34,0.05)`；其余靠发丝线分层。

## 布局与响应式

- 左固定侧边栏 232px（米白底、品牌字标在顶部、导航纵向），右侧顶部 64px 页头（页面标题 + 店主账户/登出）
- 内容区最大宽度 1200px，卡片栅格；移动端侧边栏折叠为抽屉
- 表格行高 56px，斑马纹不用，hover 行底 `#FBF9F5`

## 组件规范

- 主按钮：爱马仕橙实心、白字、8px 圆角；次按钮：白底发丝边框 + 二级文字色
- 状态标签：浅底色 + 同色系深色字（如已完成 = 浅墨绿底），不使用描边胶囊以外的形状
- Switch/选中态使用主色；输入聚焦环为 2px 橙色 12% 透明
- 删除操作用 AlertDialog 二次确认；保存成功 sonner toast，文案一句话陈述结果

## 交互与动效

- 统一 150–250ms `cubic-bezier(0.22, 1, 0.36, 1)`，无弹跳无炫技
- Hover：底色微沉或极轻微位移（≤1px）；图片卡 hover 图片 scale 1.02（300ms，overflow hidden）
- 列表删除/上下架即时反馈并局部刷新；表单防重复提交（loading 禁用）
- 空状态：居中细线线性图标 + 一句辅助色文案 + 一个主操作按钮

## 可访问性与文案

- 正文与背景对比度 ≥ 4.5:1；橙色按钮文字用白色保证对比度
- 文案：冷静、礼貌、短句；用「下架商品」而非「你确定要删吗？」；状态用行业用语（制作中/配送中）
- 金额统一 ¥ 前缀两位小数；日期 `YYYY-MM-DD HH:mm`

## 设计禁忌

- 不要渐变背景/渐变按钮；不要蓝紫科技色与霓虹发光
- 不要大面积铺橙色；不要重投影、玻璃拟态
- 不要用 emoji 充当功能图标（统一 lucide 线性图标，1.5px 线宽）
- 不要圆角超过 12px、不要全大写宽字距以外的花式排版
